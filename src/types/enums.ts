/**
 * 状态(1-正常 0-禁用)
 */
export enum EnableStatus {
    enable = 1,
    disabled = 0
}

/**
 * 菜单类型(1-菜单 2-目录 3-外链 4-按钮)
 */
export enum MenuTypeEnum {
    'MENU' = 1,
    'CATALOG' = 2,
    'EXTLINK' = 3,
    'BUTTON' = 4
}
